#! /usr/bin/env python
# encoding: utf-8

bld.program(
    features='cxx',
    source=bld.path.ant_glob('visualizer.cpp'),
    target='visualizer',
    use=[
        'kodo_visualize',
        'kodo_includes',
        'boost_system',
        'boost_program_options',
        'boost_filesystem',
        'SDL2'
    ])

bld(rule='cp ${SRC} ${TGT}', source='PressStart2P.ttf', target='font.ttf')